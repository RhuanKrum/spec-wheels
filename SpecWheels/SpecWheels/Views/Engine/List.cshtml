@model List<SpecWheels.Models.Engine.EngineModel>

@{
    ViewBag.Title = "List Engine";
}

<fieldset class="form-group">
    @Html.AntiForgeryToken()
    <h4>Engine List</h4>
    <hr />
    @Html.ValidationSummary()

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">Brand</th>
                <th scope="col">Horse Power</th>
                <th scope="col">View</th>
                <th scope="col">Edit</th>
                <th scope="col">Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td>@item.Brand</td>
                <td>@item.HorsePower</td>
                <td>
                    @Html.ActionLink("View", "View", "Engine", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Engine", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete", "Engine", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>

            </tr>               
            }
        </tbody>
    </table>
</fieldset>
    