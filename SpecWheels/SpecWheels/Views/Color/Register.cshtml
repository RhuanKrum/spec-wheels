@model SpecWheels.Models.Color.ColorModel

@{
    ViewBag.Title = "Register";
}

@using (Html.BeginForm("Register", "Color", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{

    <fieldset class="form-group has-error">
        @Html.AntiForgeryToken()
        <h4>Register Color</h4>
        <hr />
        @Html.ValidationSummary()

        <div class="form-group">
            @Html.LabelFor(model => model.Name, new { @class = "form-control-label" })
            @Html.TextBoxFor(model => model.Name, new { @class = "form-control" })
            @Html.ValidationMessageFor(x => x.Name)
        </div>

        <div id="messages"></div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" class="btn btn-primary" value="Register" />
            </div>
        </div>

    </fieldset>
}