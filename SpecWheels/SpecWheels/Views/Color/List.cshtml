@model List<SpecWheels.Models.Color.ColorModel>

@{
    ViewBag.Title = "List Color";
}

<fieldset class="form-group">
    @Html.AntiForgeryToken()
    <h4>Color List</h4>
    <hr />
    @Html.ValidationSummary()

    <table class="table">
        <thead>
            <tr>
                <th scope="col">Id</th>
                <th scope="col">Name</th>
                <th scope="col">View</th>
                <th scope="col">Edit</th>
                <th scope="col">Delete</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
            <tr>
                <th scope="row">@item.Id</th>
                <td>@item.Name</td>
                <td>
                    @Html.ActionLink("View", "View", "Color", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", "Color", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>
                <td>
                    @Html.ActionLink("Delete", "Delete", "Color", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>

            </tr>               
            }
        </tbody>
    </table>
</fieldset>
    